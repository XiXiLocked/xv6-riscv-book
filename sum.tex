\chapter{总结}
\label{CH:SUM}

本文通过逐行研究一个操作系统——xv6，介绍了操作系统中的主要思想。部分代码行体现了这些主要思想的精髓（例如，上下文切换、用户/内核边界、锁等），每一行都至关重要；其他代码行则用于演示如何实现某个特定的操作系统思想，这些实现完全可以采用不同的方式（例如，更好的调度算法、更好的磁盘上表示文件的数据结构、更好的日志机制以支持并发事务等）。所有这些思想都是在一种非常成功的系统调用接口——Unix 接口的背景下进行阐述的，但这些思想同样适用于其他操作系统的设计。
